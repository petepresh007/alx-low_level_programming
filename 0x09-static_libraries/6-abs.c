#include "main.h"
/**
 * _abs - function to print absolute value
 * @n: check for the absolute value
 * Return: absolute value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}

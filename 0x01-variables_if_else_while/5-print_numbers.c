#include <stdio.h>
/**
 * main - function for numbers in base ten
 * Return: 0 (success)
 */
int main(void)
{
	int x = 0;

	while (x < 10)
	{
		printf("%d", x);
		x++;
	}
	printf("\n");
	return (0);
}
